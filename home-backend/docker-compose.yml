version: '3.9'

services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: development
      APP_DEBUG: "true"
      DB_DSN: sqlite:/tmp/home-backend.sqlite
      JWT_SECRET: change-me-in-production
    volumes:
      - .:/var/www/html
